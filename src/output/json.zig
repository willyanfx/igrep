const std = @import("std");

/// Placeholder — JSON output is currently handled by printer.zig's JsonPrinter.
/// This module will be expanded in Milestone 5 for richer JSON output
/// (structured match objects, metadata, statistics).

test "json module placeholder" {}
